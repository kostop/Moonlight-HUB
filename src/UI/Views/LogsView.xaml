<UserControl x:Class="MoonlightHub.UI.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="0,4,0,10">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <CheckBox x:Name="AutoScroll" Content="自动滚动" IsChecked="True" Margin="0,0,14,0" />
                <Button Content="打开日志文件" Style="{StaticResource GhostButton}" Click="OpenFile_Click" />
                <Button Content="打开日志目录" Style="{StaticResource GhostButton}" Click="OpenDir_Click" Margin="0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="来源" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox x:Name="Source" Width="260" SelectionChanged="Source_Changed" />
            </StackPanel>
        </DockPanel>
        <Border Style="{StaticResource Card}" Padding="0" Margin="0">
            <TextBox x:Name="Text" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="{StaticResource MonoFont}" FontSize="12" Background="Transparent" BorderThickness="0" Padding="14" TextWrapping="NoWrap" />
        </Border>
    </DockPanel>
</UserControl>
